{
  "name": "cljest",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "jest:watch": "jest --watch",
    "jest:ci": "CI=true jest --ci",
    "jest:server": "clj -X cljest.compilation/watch",
    "jest:compile": "clj -X cljest.compilation/compile",
    "publish": "clj -M -m deps-library.release"
  },
  "author": "Pitch Software GmbH",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.34.0",
    "jest": "^29.2.1",
    "jest-junit": "^14.0.1",
    "jest-preset-cljest": "file:../jest-preset-cljest",
    "prettier": "^2.8.4"
  }
}
